#include <bits/stdc++.h>
using namespace std;
#define MAXN 6

int dp[MAXN][2][10];

int main() {
	int T, len;
	scanf("%d", &T);
	while(T-->0) {
		scanf(" %s", num);
		len = strlen(num);
	}
	return 0;
}